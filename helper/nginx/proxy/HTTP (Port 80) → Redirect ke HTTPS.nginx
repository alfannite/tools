server {
    listen 80;
    server_name your-domain.local;

    # Security: block direct IP access (optional)
    if ($host != "your-domain.local") {
        return 444;
    }

    # Redirect all traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}