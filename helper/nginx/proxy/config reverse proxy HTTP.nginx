server {
    listen 80;
    server_name router.faybee.io;

    location / {
        proxy_pass http://IP-MIKROTIK:8081;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
}